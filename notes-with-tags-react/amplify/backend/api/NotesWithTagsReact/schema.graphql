type Tag @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String
  noteID: ID! @index(name: "byNote")
}

type Note @model @auth(rules: [{allow: public}]) {
  id: ID!
  header: String
  content: String
  Tags: [Tag] @hasMany(indexName: "byNote", fields: ["id"])
}
 